# Example environment variables for e2e tests

# --- OpenAI Configuration ---
# Required: Your OpenAI API Key
OPENAI_API_KEY="sk-..."

# Optional: OpenAI Base URL (if using a proxy or alternative endpoint)
# OPENAI_BASE_URL="https://your.openai.proxy/v1"

# --- Google Configuration ---
GOOGLE_API_KEY="..."

# --- Langfuse (Optional - Required for Langfuse tests) ---
LANGFUSE_PUBLIC_KEY="pk-lf-..."
LANGFUSE_SECRET_KEY="sk-lf-..."
LANGFUSE_HOST="https://cloud.langfuse.com" # Or your self-hosted instance URL
# Prompt IDs from your Langfuse project (replace with actual IDs if using prompt management)
LANGFUSE_BASE_PROMPT_ID="your_base_prompt_name" # e.g., "ha_base_prompt"
LANGFUSE_API_PROMPT_ID="your_api_prompt_name"   # e.g., "ha_api_prompt"